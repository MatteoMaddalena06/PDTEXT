Attention, GIT HUB has changed the encoding of the file from ASCII to UTF-8 this will cause problems when the masm compiler does not support this encoding, it will keep translating into ASM and the allocated data will be different,
(CONVERT BOTH FILES TO ASCII !!!!)
